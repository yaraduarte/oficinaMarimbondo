apiVersion: v1
kind: Service
metadata:
  name: oficina-api-service
  namespace: oficina-marimb0ndo
spec:
  type: NodePort
  selector:
    app: oficina-api
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30080
